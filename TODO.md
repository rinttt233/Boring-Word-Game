# 待办任务

> **只放未完成的事。** 已完成批次与逐项细节见 [`CHANGELOG.md`](CHANGELOG.md)（每版「速览」）、
> [`docs/roadmap.md`](docs/roadmap.md) §六 执行记录；平衡/内容清单见
> [`docs/balance_proposals.md`](docs/balance_proposals.md)。
> 标记：`[ ]` 未做 · `[~]` 需先拍板 · `[x]` 只出现在「决策记录」里。
> **维护约定**：做完就从本文件删掉（细节已进 CHANGELOG），别让它长成历史堆。

## 📦 版本与发布规则（重要）

> 当前版本：**1.3.2**（盲测 + 体验优化）。详见 `CHANGELOG.md`、`docs/versioning.md`。

1. **不擅自发版**：只有明确指令（"发版 X.Y"/"生成版本号"）才生成版本号；日常改动只做批次备份（`_refactor_backup/<批次>_<时间戳>/`）。
2. **主动提醒**：改动足够大（新系统/新玩法/大批内容）时 agent 主动提醒"建议发版"，等用户点头。
3. **发版必附更新报告**：`CHANGELOG.md` 条目（速览 + 折叠详情）+ 归档 `_refactor_backup/vX.Y_<时间戳>/`。
4. **递增语义**：`X.Y` 大批次（次版本+1）；`X.Y.Z` 小修补（修订号+1）。
5. **一键执行**：`python -X utf8 release.py <版本> "<主题>" [--check]`（归档 + 写 VERSION + 插 CHANGELOG + 复制快照，幂等）。
6. **发布快照（规则第 10 条）**：发版后复制整个工作区到 `releases/v<版本>/`，**排除该文件夹自身**（以及 `saves/`、`_refactor_backup/`、`__pycache__/`）；同名版本重发先清空再复制。

## 🎯 下一步建议（推荐顺序）

1. **批次5 · 回收/拆解生态**（A6 + B4 + B5）—— 路线图下一个机制批次，但**动手前先定 A6 口径**（见「待决策」）。
2. **B3 胜利结算画面** —— 一小时级，补上通关体验的收尾（目前只有一行日志）。
3. **批次6 · A8 环境事件预警** —— 接着批次4 的环境页做（事件提前预警 + 加固设施）。

## 🚧 已排入批次（未开工）

- [ ] **批次5 回收/拆解生态**：A6 残骸产出改"合金 + 部件"（⚠ 待决策）、B4 部件加工链、B5 设施拆解返还。
- [ ] **批次6 数值打磨**：A8 环境事件**预警时间线** + 加固设施。（A7「品位」已由 P1 ① 以"决定采掘速率"落地；若还要"下游产物档位"需另议。）
- [ ] **批次7 扩展层**：B7 远圈风险 + 维修件、B8 圈层物流（目前圈层物流惩罚**只显示**，UI/向导已标"预留"）。
- [ ] **批次8 结算与 QoL**：A10 结算统计评级、D3 物料按圈层/设施/品位分解、D4 统计窗口化、D6 通知中心（完整版）、D7 教程勾选清单、D8 快捷键覆盖层。（D5 存档槽 UI 已完成。）

## ❓ 待决策（需你拍板）

- [~] **批次5 · A6 残骸口径**：现在"残骸拆空会**返还一个执行单元**"，而 A6 建议"残骸不再白给单元、改走部件加工链" —— 两者冲突，二选一：① 保留返还（现状，回收线更友好）② 改部件链（A6 原意，单元更稀缺）。

## 🔧 UI / QoL 小项（未选，随时可插）

- [ ] 字号缩放（Ctrl +/-，适配分辨率与视力）
- [ ] 建筑分组名入数据（`BuildingsPanel._group_name` 硬编码 → 设施 def 的 `group` 字段）
- [ ] 胜利结算画面（= 批次8 A10；回合 / 资源 / 固化条目统计）
- [ ] 电力页再加"发电机组按优先级档位分组"明细（批次4 已给降载档位与储能，属锦上添花）

## ⏸ 未选备选（本轮不做，保留）

- [ ] C4 知识一致性压力（完整版：条目越多恢复越慢/越贵）
- [ ] C6 前哨站 / 第二基地（跨圈物流 + 双电力，2+ 批次的大工程）
- [ ] C1b 恢复树再扩并行分支（氯碱 / 炸药 / 石油之外）
- [ ] 真人通关时长校准（`balance_probe.py` 已有机制验证，缺真人数据）

## 🚫 明确不做（决策记录，别再提）

- **C5 难度/模式选择**：用户选单一难度。
- **B1 单元制造厂 / B2 知识固化奖励单元**：已由 P1 ② 以「执行单元装配厂 + 每固化 2 条主线 +1 单元」落地。
- **E 组**：敌人/战斗、无限科技树、默认仓库上限与复杂物流、随机刷新地块、副产物市场出售。

## 📌 长期执行

- [ ] WIKI-3：新增内容阶段时补**叙述性**词条（自动词条已随 `content/` 同步，零维护）。

## ✅ 已完成（索引；细节见 CHANGELOG / roadmap）

- 批次 0 / 1 / 1.5 / 2 / 3（基线测量 · 单元效能 · 建造耗时 · 记忆与维护压力 · 副产物积压+多出口）→ **v1.3 / v1.3.1**
- P0 试玩报告补丁（5 项，含 CRASH-1 / MIGRATED-1 / POWER-1 / PLOT_BUILD-1 / READLOCK-1）→ **v1.3.1**
- P1 结构性调整（品位接入产出 · 单元扩容 · `demolish` 拆除复用 · 记忆运转口径）→ **v1.3.1**
- AI 可玩性包（`AGENTS.md` / `report` / `suggest` / `cover` / `agent_play` / `blind_test`）→ **v1.3.1**
- 盲测阶梯策略（`tools/ladder_policy.py`，**公平通关 20/20**）+ `cover` 的 `bool` 判断修复 → **v1.3.2**
- BUG-5 临时知识窗口（180s + `fixate` 排队 + 倒计时告警）→ **v1.3.2**
- **批次4 电力优先级 + 储能**（`power_priority` 降载 · 电池组/蓄热罐 · 电力页时间线 · 环境页倍率）+ **A13**（电网 400kWh 上限 + 自放电）+ **A12 ①③**（煤 120 + 圈1 首探保底煤矿）+ **A14**（开局 4 单元）→ 待发版
- 技术线路 TL-0 / PW / TL-A~E（副产物生态、发电体系、电气化、石化、稀有金属、稀土催化、电子学）→ `docs/tech_blueprint.md`
- 百科程序 WIKI-1~5（232 条词条 · 轻量排版 · 双击跳转）→ **v1.2**
- UI/UX 三十余项（面板分组/筛选/诊断页/快捷键/存档槽 D5/日志分级/警告看板…）→ 散见各版本
