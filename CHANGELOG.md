# 更新报告 / CHANGELOG

> 版本规则：`X.Y` = 大修改批次（新系统/新玩法/重要架构）；`X.Y.Z` = 小修补（bug 修复/文案/平衡微调）。
> 版本号只在收到明确指令时生成，生成时同时产出本文件对应条目与归档备份（`_refactor_backup/vX.Y_<时间戳>/`）。
> **发版后还会把整个工作区复制到 `releases/vX.Y/`（工作区内独立文件夹，复制时排除该文件夹自身以防递归嵌套）。**
> 历史基线：1.0 / 1.1 为开发期里程碑，未正式归档；正式记录始于 1.2。

---

## v1.2 — 百科程序完成（WIKI 全流程）

**日期**：2026-09-08
**归档**：`_refactor_backup/v1.2_<时间戳>/`
**发布快照**：`releases/v1.2/`（工作区内独立文件夹，已排除自身/存档/备份/缓存）
**主题**：把散落在各处的规则、配方与教材知识，汇成"查得到、看得懂、能排障、随处可查"的游戏内百科。

### 速览

- **游戏内百科**：`systems/wiki.py` 数据层 + GUI 百科页 + 控制台 `wiki`；自动词条 161 条 + 手写 53 条。
- **词条轻量排版**：标题加粗、来源灰字、反应式独立缩进等宽。
- **双击跳转百科**：建筑 / 地块 / 恢复树 / 溯源 / 日志行各有对应词条。
- 控制台：`wiki`（列分类）/ `wiki <关键词>`（搜索）/ `wiki #<id>`（直查）。

<details>
<summary>详细更新报告（点击展开）</summary>


### 一、百科数据层（新模块）
- 新增 `systems/wiki.py`（注册为 `wiki` 模块，GUI 与控制台共用同一实例，纯派生数据不落存档）。
- **自动词条 161 条**，运行时从现有 content 生成，与游戏内容天然同步、零维护：
  - 资源 60：类别/单位/**热值**/**燃料类别(solid/liquid/gas)**/**谁产出、谁消耗**
  - 建筑 43：建造成本/占地/单元位/建造耗时/**配方速率与副产物**/燃烧参数/可再生额定功率/**解锁科技**
  - 配方 35：输入→输出速率、副产物、承载设施
  - 科技 23：恢复与固化成本、前置依赖、解锁设施、**是否主线或 optional 分支**
- **手写词条加载**：`content/wiki/*.json` 按文件名合并（`intro/tutorial/stages/faq/science/materials`）。

### 二、手写词条 53 条（分两批）
- **设定 7**：世界与处境、记忆劣化、恢复机制、终局、执行单元瓶颈、勘探与地块、环境与昼夜。
- **教程 14**：6 篇上手教程（开局三件事/先解决电/别让知识丢掉/跟工艺链走/界面速查/F11 调试）+ 8 篇 FAQ，
  FAQ 统一按「症状 → 原因 → 处置」组织（停摆排障/缺电处理/条目不丢/建不了设施/产量不达标/加速与巡航/存档读档/勘探补偿与误差）。
- **阶段导读 8**（新分类「阶段」）：立足煤钢、电网与可再生、电气化铜铝、石化、稀有金属、稀土催化、电子学、终局迁移，每条含关键耦合与常见卡点。
- **科普 24**：12 篇工艺科普（干馏、高炉、炼钢不锈钢、接触法硫酸、合成氨、石油分馏、裂化加聚、电解铜铝、稀土萃取、催化剂、西门子法、能源热值）+ 12 张**教材式物质卡片**（煤与焦炭、生铁与钢、硫酸、氨、烃三兄弟、四种电气金属、稀土十七兄弟、硅的提纯长跑、氢、石灰石、催化剂、热值与热机）。

### 三、界面
- 右区新增「百科」页：搜索框 + 分类树（设定/教程/阶段/科普/资源/建筑/配方/科技）+ 可滚动详情。
- **词条轻量排版（WIKI-4）**：标题加粗、来源与标签灰字、**反应式独立缩进+等宽+上下留白**、要点缩进、小节标题加粗；正文原有 Unicode 下标（H₂SO₄ / [-CH₂-CH₂-]ₙ）直接可读。
- **双击跳转百科（WIKI-5）**：统一入口 `GuiUI.open_wiki()` + 面板注入回调。
  - 建筑树双击 → 设施词条｜地块树双击 → 矿脉物质词条｜恢复树双击 → 科技词条｜溯源资源双击 → 物质词条
  - **日志行双击 → 智能匹配**：①标题出现位置最靠前（同位置取更长＝句子主语优先）②`[物质id]`括号写法 ③标签关键词兜底（"记忆崩溃"→记忆劣化条目）；无命中时给提示。
- 控制台命令：`wiki`（列分类）｜`wiki <关键词>`（按相关度搜索）｜`wiki #<id>`（直查）。

### 四、同期合并的重要改动（自 1.1 起累计）
- **技术线收口**：TL-B 石化（蒸馏/裂解/聚合）、TL-C 稀有金属（每金属完整链：选矿→电弧炉→金属→不锈钢/高温合金）、TL-D 稀土催化（独居石→萃取→钸镧钕→催化剂→升级配方）、TL-E 电子学（石英砂→工业硅→TCS→西门子法多晶硅）。主线并入 db_petrol/db_distill/db_copper/db_refractory/db_rare_earth。
- **电力体系扩展**：新增 `daylight` 昼夜模块（60s 周期）；设施级燃料类别白名单；分级火电（小型固炉/大型燃煤/流体电站）；可再生（光热/光伏/风电）随昼夜与事件调制；环境事件新增 solar/pv/wind 专属倍率。
- **勘探圈内补偿**：按"圈内实际构成 vs 配置目标"动态修正抽取权重，稀缺类型更易被探到，配比长期收敛到目标。
- **系统修复**：非原子扣费（建造/恢复/固化/数据库/记忆加固失败不退料）已修为"失败退还已扣项"；覆盖存档迁移校验、胜利链同步扩展。
- **UI/UX 批次**：标签条自动换行堆叠；长信息折行（终端按显示宽度折行、GUI 日志折行开关）；建造撤销（Ctrl+Z，已产出不可撤销）；建筑分组维度切换（类型/地块/圈层/状态）；一键补员与只看停摆；圈层构成仪表（含勘探补偿强度）；物料平衡总览（与电力页组成双诊断台）；电力专页；日志等级过滤与点击跳转；命令 Tab 补全；窗口几何持久化；符号统一与字体回退；高 DPI 与滚轮一致性；F11 调试面板。
- **测试资产**：单测 70+ 项（含 wiki 7 项、电力 8 项、勘探补偿 4 项）；端到端脚本 smoke/gameplay/chain/tlb/tlc/tld/tle/power/victory 全部通过。

### 五、验证
- `python -X utf8 -m unittest discover -s tests`：通过
- `python -X utf8 main.py --gui-selftest`：通过
- 端到端：smoke / gameplay / chain / tlb / tlc / tld / tle / power / victory 全绿
- 百科条目总数 **214**（自动 161 + 手写 53）

</details>

---

## v1.3 — 单元效能 · 建造耗时 · 记忆与维护压力

**日期**：2026-09-12
**归档**：`_refactor_backup/v1.3_20260912-192534/`
**发布快照**：`releases/v1.3/`（工作区内独立文件夹，已排除自身/存档/备份/缓存）
**主题**：把"中后期只剩排队"的三处根因补齐 —— **执行单元不再只有数量、建造要花时间、记忆与设备需要持续维系**。

### 速览

- **单元效能 ×1.00~×2.50**：调度类知识同时提高产能与作业速度；条目丢失立刻回落。
- **建造要时间**：`build` 变成占一个执行单元的施工耗时作业（5~30 秒），`undo` 可全额退料。
- **记忆压力随规模增长**：劣化 = 基础 + 固化条目数项 + 运转设施数项；`maintain` 分档恢复。
- **维护件体系**：维护站生产；运转 100% / 待机 33% / `mothball` 0%；断供会故障停机。
- 基线测量工具 `tools/cost_audit.py`（只读）→ `docs/baseline_report.md`。
- 单测 **123 例**。

<details>
<summary>详细更新报告（点击展开）</summary>

**背景**：v1.2 之后做了一轮同类项目调研（Factorio / Captain of Industry / ONI / FPS 统计改进等），
并把结论落成清单与排序：`docs/balance_proposals.md`（清单）、`docs/roadmap.md`（批次顺序）、
`docs/baseline_report.md`（由 `tools/cost_audit.py` 自动生成的基线测量）。


### 一、主要改动

**1. 单元效能（C1 / A5 / B3，批次 1）—— 单元瓶颈的第二条轴**
- `core/units.py`：新增 `UnitPool.efficiency`（默认 1.0，随存档读写、旧档缺字段按 1.0）、
  `set_efficiency()`、`effective_slots()`、`staff_factor(assigned, slots)`。
- 加成公式：`staff_factor = 1 + (ef − 1) × min(1, 已派员/槽位)` —— **ef=1.0 时恒为 1.0，严格保持旧基线**；
  派员不足只拿部分加成，堵住"塞 1 个单元当满员"的漏洞。
- 语义：**产能与节拍倍率**（不是字面意义的槽位合并）。基线测量显示 43 座设施里只有 3 座是多槽，
  槽位折算几乎无效，故改为"同样单元数产出更多、作业更快"。
- 知识来源：`content/recovery.json` 新增 4 条 `grants.unit_efficiency` 条目 ——
  执行单元总线复用 +0.25、指令预取 +0.25（可选）；**并行作业编排 +0.5、单元自主决策 +0.5**；
  效能上限 1.0 → 2.5。其中「总线复用」「并行作业编排」为**主线**（决策：效率科技必须撑起中后期）。
- 生效时机：恢复即生效（与"设施解锁只需恢复"一致），**记忆崩溃/条目过期会立刻回落**并写 warn 日志。
- 作业提速：恢复/固化/加固的作业耗时 ÷ 效能。
- UI：单元面板显示"效能 ×N / 在役·运转·空闲"；建筑详情显示"效能 → 产能·节拍倍率与满负荷所需单元数"；
  百科科技词条新增「效果」行（自动读取 grants）。

**2. 建造耗时作业（A11，批次 1.5）—— 建造也要占用执行单元**
- `build` 改为施工耗时作业：先判空闲单元（无 → 拒绝且**不扣资源**）→ 扣材料（失败退料并归还单元）
  → 开 `kind="build"` 作业，时长 = 设施 `build_time`（5~30 游戏秒）。
- 建造中：不产出、不耗料、**不计入电力预算**、不可分配单元、不可封存；完工不自动派员。
- 撤销（`undo`/Ctrl+Z）：施工中会中止作业、回收施工单元、全额返还材料并还原地块；已产出设施仍禁止撤销。
- 读档自愈：作业丢失时会自动置为完工。
- 调试旁路：`dbg instant [on|off]` 与 `install_instant()`（端点脚本用它聚焦产线数值，
  `gameplay_test` 走真实耗时路径）。

**3. 记忆压力随规模增长（A1 / C4，批次 2a）**
- `content/memory.json`：`劣化 = 0.02/s + 0.002/s × 已永久固化条目数 + 0.004/s × 运转设施数`。
- 加固恢复改为**按完整度分档**（<40 → +32；<70 → +25；否则 +16），越接近满值回报越低。
- 实测（`docs/baseline_report.md`）：空厂 83 分钟归零 / 2.9 次加固·小时；
  中期（8 条 10 台）22 分钟 / 10.9 次；终局（13 条 22 台）**12 分钟 / 19.3 次**。
- 压力台阶变化时限频提示（"劣化速率上升至 …"），状态栏显示当前劣化速率。

**4. 设备维护与待机消耗（A2 / C3，批次 2b）—— 规模是有代价的**
- 新增模块 `systems/maintenance.py` + 配置 `content/maintenance.json`；
  **知识门控**：恢复主线条目 `db_upkeep`「设备维护与检修工艺包」后体系才启用（前期不背维护包袱）。
- 新增内容链：物质 `maintenance_kit`「维护件」+ 配方 `make_maintenance`（钢 0.05/s + 残骸合金 0.12/s → 0.3/s）
  + 设施 `maintenance_depot`「维护站」（造价 残骸 20 + 钢 8，需 db_upkeep）。
- 消耗规则：**运转 100% / 停着（无单元）33% / 封存 0%**。供得上 → 设备状态 +0.5/s 回升；
  供不上 → −0.03/s 下滑 → 低于 75 起按概率故障（归零时约 50%/分钟）→ **故障停机 25s**
  → 抢修恢复至 40（仍易故障，不修会反复）。
- **封存/解封**：`mothball <设施> [on|off]` 命令 + 建筑面板按钮；封存释放单元且零消耗，
  解封需一个空闲单元 + 10s 重启。
- 经济账：每 10 台运转设施约需 0.10/s（360 件/小时），一台维护站（含效能加成）覆盖 15~30 台。
- UI：状态栏新增「维护件 x.x (需求/s) 设备最低状态」（低于 60s 需求转告警配色）；
  建筑行/详情显示设备状态、停机剩余、封存标记；`memory` 命令追加维护摘要。

**5. 基线测量工具（A9，批次 0）**
- 新增 `tools/cost_audit.py`（**只读**，`--write` 生成报告）：成本折算（设施·秒/单元·秒）、
  单元槽位覆盖与效率档实验、记忆压力预算、维护件收支四张表 → `docs/baseline_report.md`。

**6. 顺带修复**
- **停摆状态逐 tick 抖动**：`industry.tick` 原先每 tick 先无条件清除停摆状态、再由各 tick 函数重新判定，
  导致"停摆↔恢复"日志与统计虚增（实测 40 tick 产生 80 次停摆事件）；改为由各 tick 函数报告真实状态。
- **提取设施部分缺电仍满产**：`_tick_extractor` 现在按可用电力比例减产（与配方设施语义一致）。
- **非原子扣料**（v1.2 遗留排查项）：建造失败路径统一"退料 + 归还单元"。

### 二、影响范围
- **玩法**：中后期不再"只剩排队"。单元效能提供第二条成长轴；建造需排队调度；
  记忆压力全程有效；恢复 `db_upkeep` 后设施开始吃维护件，扩张有持续成本，
  并给出"加维护站 / 停用 / 封存"三种应对。
- **内容**：主线恢复条目 **10 → 13 条**（新增 db_unit_bus、db_unit_parallel、db_upkeep）；
  新增 1 物质、1 配方、1 设施；百科词条 **214 → 224**（新增 设定《维护：规模是有代价的》、
  教程《维护件供不上怎么办》，并重写 记忆劣化 / 执行单元 / 停摆排障 / 建造排障）。
- **界面**：状态栏 +1 段（维护件/设备状态）；建筑面板新增 封存/解除封存 按钮与
  "建造中 剩余 Ns""设备状态""停机剩余"显示；分组维度新增"建造中"状态组；
  `help`/`dbg` 帮助与 `memory` 输出更新。
- **端点脚本**：`gameplay_test` 改走真实建造耗时路径；`chain/tlb/tlc/tld/tle/power/victory`
  使用 `instant_build` 旁路；`victory_test` 覆盖"恢复 db_upkeep → 建维护站 → 产维护件"。
- **存档兼容**：全部新字段（`units.efficiency`、`facility.under_construction/halt_until/halt_reason/mothballed/upkeep`、
  `engine.stats`）均有默认值，**旧档可直接读取**，已由测试覆盖。
- **测试**：单测 93 → **123 例**（新增 `tests/test_unit_efficiency.py` 16、`tests/test_build_time.py` 13、`tests/test_pressure.py` 17）。

### 三、验证
- 单元测试：`python -X utf8 -m unittest discover -s tests` → **Ran 123 tests, OK**（发版门禁 `--check` 通过）。
- GUI 自检：`python -X utf8 main.py --gui-selftest` → 通过。
- 端到端脚本全 OK：`smoke_test`、`gameplay_test`、`chain_test`、`power_test`、
  `tlb_test`、`tlc_test`、`tld_test`、`tle_test`、`victory_test`
  （胜利链 13 条主线全固化 → 可靠数据库竣工、劣化终止）。
- 关键数值抽查：焦炉 ef=1.5 时产出/耗煤/耗电同乘 ×1.5（配比不变）；丢条目后回落 ×1.25；
  建造 8s 完工且期间零产出零耗电；维护件 0.3/s 供给 0.15/s 需求；封存后需求归零、
  解封 10s 重启后复产。
- 备份：`_refactor_backup/v1.3_20260912-192534/`（发版归档）、
  `_refactor_backup/unit_eff_20260912-172347/`（批次 1 前）、
  `_refactor_backup/batch2_pressure_20260912-174125/`（批次 2 回滚点）。

</details>

---

## v1.3.1 — 品位接入 · 单元扩容 · 拆除复用 · 记忆口径 · 副产物积压

**日期**：2026-09-13
**归档**：`_refactor_backup/v1.3.1_20260913-075914/`
**发布快照**：`releases/v1.3.1/`（工作区内独立文件夹，已排除自身/存档/备份/缓存）
**主题**：把另一会话 AI 通关试玩报告里的**缺陷**与**结构性瓶颈**一次修完 —— 并让"品位/圈层/单元/副产物"这些原本只是显示或无限堆积的东西，真正参与玩法。

### 速览

- **5 个 P0 修复**：`db` 命令崩溃；`report` 通关字段（`migrated` / `can_migrate` / `victory`）语义；电力汇总把停摆机组算作产电；`developed`/`depleted` 地块误报；读档可能落入「无煤无电」死锁（新增一次性应急煤）。
- **品位接入产出**：`产出 = 标称 × clamp(品位/参考品位, 0.25, 2.0)`。
- **执行单元可扩容**：主线 `db_units` 解锁装配厂（1 个 / 200 秒）＋ 每固化 2 条主线 +1 单元（上限 +6）。
- **拆除与复用**：`demolish <设施>` 返还建造成本 50%；枯竭地块可重建。
- **副产物积压限产** + 4 种应对（转化 / 放空塔 / 回注井 / `policy backlog ignore`）。
- AI 试玩入口成型：`AGENTS.md`、`agent_play`、`blind_test`、`report`/`suggest`/`cover`；单测 **181 例**。

<details>
<summary>详细更新报告（点击展开）</summary>

**说明**：按你的指令以 **1.3.1（小修补）** 发布；但本次改动含新内容与新机制（主线 13→14 条），
若你更希望它作为大版本，可在下次发版时把版本号语义调整为 1.4（内容与快照无需改动）。


### 一、主要改动

**1. P0 试玩报告补丁（缺陷修复，不改平衡）**
- **CRASH-1**：`ui/commands.py:_cmd_db` 在"5/5 竣工但尚未迁移"时对 `next_project()` 的 None 取下标崩溃 → 判空，
  并输出"全部子系统已竣工，可执行 migrate"。
- **MIGRATED-1**：`DatabaseSystem` 新增 `migrated`（随存档，旧档用 `complete` 兜底）；`report` 中
  `can_migrate`（=子系统建完）与 `migrated`/`complete`（=已迁移）语义分离；**顶层新增 `victory`**；
  劣化终止后 `degrade` 输出 0.0 并保留 `degrade_rated`。
- **POWER-1**：`power_balance` 排除**停摆**设施（原先把"没燃料的电站"仍算作产电，导致面板"盈余却全厂缺电"）；
  `produce/consume` = 实际值，另给 `produce_rated/consume_rated`。
- **PLOT_BUILD-1**：建造判定改为**先看该地块有没有设施**（`developed` 有设施 → "该地块已有设施"，
  不再误报"尚未占领"）；**depleted（采空）地块允许重建**。
- **READLOCK-1**：新增 `IndustrySystem.power_deadlock()` 体检；读档后自检并在死锁时
  **一次性发放应急煤 30t**（记入 `stats.emergency_starts`，一局只发一次）；`agent_play` 启动也做同一体检。
- **LOCK_RULE-1（部分）**：`Facility.stall_code` 机器可读停摆码（`no_input/no_power/no_fuel/no_fuel_set/
  fuel_class/no_recipe/no_capacity/knowledge_lost/halted/no_unit_rate/sink_full/...`），`report` 以 `reason_code` 输出。
- `report` 增 `seed`（`main.py --seed N` 写入引擎）、`command_count`、`deadlock` 字段。
- 另修：`report` 在"维护启用但无设施"时 `round(None)` 崩溃；`victory_test` 未锁天气导致的偶发失败。

**2. P1 结构性调整（按用户决策）**
- **①品位接入产出**：`产出 = 标称 × clamp(grade/ref_grade, 0.25, 2.0) × 效能 × 环境 × 供电比例`；
  `ref_grade` = 各矿区间中位值（14 种，写进 `content/substances.json`）。
  中位品位＝标称产量，贫矿最多 −75%、富矿最多 +100%；**物流惩罚仍只显示**（UI/向导/FAQ 已标注"预留"）。
- **②单元扩容**：新主线 `db_units`（第 14 条）解锁「**执行单元装配厂**」（钢 0.08/s + 铜 0.05/s + 电 0.6/s，
  0.005 单元/s = 1 个 / 200 s，受单元效能加速）；**每永久固化 2 条主线条目 +1 执行单元（上限 +6）**。
- **③拆除与复用**：`demolish <设施id>`（建筑面板亦有按钮）返还 **50%** 建造成本、释放单元、地块恢复可建；
  施工中拆除走 `undo`（全额退料）；`depleted` 地块可重建。
- **④记忆口径修正**：`_running_facility_count` 只统计**真正运转**的设施（排除停摆/停机），
  与文档一致；`per_fixated_entry` 0.002 → **0.003** 作压力补偿。

**3. 批次3：副产物积压 + 四种应对**
- 新增 `content/backlog.json`：阈值 焦炉煤气 400／煤焦油 200／高炉煤气 600／裂解干气 300／氢 500／钒 200；
  超限时产出它的设施按 `clamp(阈值/库存, 0.15, 1.0)` **限产**（进入/退出各记录一次日志）。
- 四种应对：**①转化** 水煤气变换炉（新设施 `gas_shift`：煤气+水→氢）；**②放空** 放空塔
  （新设施 `vent_tower`，1.5/s 销毁，随 `db_coking` 解锁）；**③回注** 回注井（新设施 `injection_well`，
  2/s 堆存、容量 2000，装满即停）；**④政策开关** `policy backlog throttle|ignore`。
- 观测与提示：`report.backlog`、物料页 `!积压` 标记与出路提示、建筑详情显示放空对象/堆存进度/因积压限产、
  `suggest` 直接建议"建哪种出路"。

### 二、影响范围
- **玩法**：品位真正决定采掘速率（勘探选址开始有意义）；单元不再是固定资源（装配厂 + 主线奖励）；
  设施可拆除重规划；副产物不再是免费堆积（不处理会限产，但有四种对策）。
- **内容**：主线 **13 → 14 条**（新增 `db_units`）；新增设施 **3 个**（装配厂 48 项设施中的新增项、
  放空塔、回注井、水煤气变换炉共 4 个：设施总数 44 → 48）；新增配方 2 条（`assemble_unit`、`shift_gas`）；
  新增物质字段 `ref_grade`（14 种）；新增配置 `content/backlog.json`；
  百科词条 224 → **232**（新增教程《副产物积压怎么办》，重写 FAQ《产量不达标》《建不了设施》）。
- **界面**：建筑面板新增「拆除」按钮；建筑详情显示品位折算产出、放空/堆存状态、因积压限产；
  物料页显示副产物积压与出路；`help` 增 `demolish`/`policy` 说明。
- **接口**：`report` 增 `victory`/`can_migrate`/`migrated`/`complete`/`degrade_rated`/`seed`/`command_count`/
  `deadlock`/`backlog` 与设施级 `reason_code`/`stored`/`unit_progress`/`backlog_over`；新增命令
  `demolish`、`policy`；盲测覆盖清单 22 → **26 项**。
- **存档兼容**：全部新字段都有默认值（`database.migrated`、`recovery.unit_bonus_granted`、
  `facility.{unit_progress,stored,backlog_over,stall_code}`、`industry.backlog_policy`），**旧档可直接读取**，已由测试覆盖。
- **测试**：单测 155 → **181 例**（新增 `tests/test_p0_fixes.py` 16、`tests/test_p1_content.py` 14、
  `tests/test_backlog.py` 12）。

### 三、验证
- 单元测试：`python -X utf8 -m unittest discover -s tests` → **Ran 181 tests, OK**（发版门禁 `--check` 通过）。
- GUI 自检：`python -X utf8 main.py --gui-selftest` → 通过。
- 端到端脚本 9 个全 OK：`smoke_test`、`power_test`、`gameplay_test`、`chain_test`、
  `tlb_test`、`tlc_test`、`tld_test`、`tle_test`、`victory_test`
  （通关链现在覆盖：**14 条主线全固化** + 副产物处置（放空塔/水煤气变换炉）+ 残骸回收供料
  + 数据库 5/5 + 烧录迁移）。
- 修复项专项验证：`db` 在"5/5 未迁移"下不再崩溃；`report.database.migrated/victory` 迁移后为 true；
  煤=0 时 `power_balance.produce = 0`（原为 1.60）；`depleted` 可重建；读档死锁触发一次性应急煤且不重复发放；
  品位折算实测：圈1 品位 1.7% 与 95% 的矿产出比值 = 0.25 : 2.00（夹取边界）；
  副产物超限时产出由 0.35/s 降至 0.17/s（限产）；放空塔 150 s 内把 500 煤焦炉煤气降到阈值以下。
- 备份：`_refactor_backup/v1.3.1_20260913-075914/`（发版归档）、
  `_refactor_backup/p0_fixes_20260913-073959/`、`_refactor_backup/p1_batch3_20260913-075537/`（回滚点）。

</details>

---

## v1.3.2 — 盲测 + 体验优化

**日期**：2026-09-13
**归档**：`_refactor_backup/v1.3.2_20260913-094317/`
**发布快照**：`releases/v1.3.2/`
**主题**：把「不读源码也能玩通」从**声称**变成**可复现证据**（公平自动通关 20/20），并修掉两个影响 AI/玩家体验的真问题（临时知识窗口、`cover` 判据）。

### 速览

- **盲测可直接通关**：新增 `tools/ladder_policy.py`（只用文档化接口），`blind_test --policy ladder` **20/20 局从零开局公平通关**（中位 47.8 游戏分钟、14 条主线全固化、记忆 100%）。
- **临时知识窗口（BUG-5）**：90 → **180 游戏秒**；`fixate` **自动排队**（材料不预扣、排队不冻结窗口）；**已开工的烧录不会中途丢失**；`entries` / GUI / `report` 给出倒计时（<30s 告警）。
- **修 `cover` 判据**：`eval` 作用域缺 `bool`，导致 `db_build` / `victory` / `env_event` 三个条目**永远判不过**。
- **修盲测误判**：业务错误（材料不足、没有空闲单元…）被当成成功，会让策略空转并饿死后续步骤。
- `suggest` 补 4 条策略建议（电力节流 / 先拆残骸 / 恢复前看固化费 / 钢富余建装配厂）；`AGENTS.md` 规则 10 → 18 条。
- 单测 **206 例**。

<details>
<summary>详细更新报告（点击展开）</summary>

**背景**：v1.3.1 之后做了两件事——① 让盲测具备"规划能力"（原来只会照建议列表走一步，长停在 11~17/26 覆盖、不通关）；② 处理外部试玩报告里剩下的 BUG-5。两份工作都只动**工具/接口/窗口参数**，没有改工艺链数值。


### 一、主要改动

**1. 盲测阶梯策略：公平自动通关（新工具）**
- 新增 `tools/ladder_policy.py`：「阶段机 + 资源账本 + 单元账本」。**只读 `report` 的 JSON 字段 + 只发游戏命令 + 只读 `content/*.json`**；不 import `core/`/`systems/`、不用 `dbg`、不注入资源、不锁天气（有专门的静态测试守住这条线）。
- 资源账本：把"通关还差多少"（数据库 5 项 + 主线恢复/固化费 + 计划内设施建造费）按配方**逐层展开**成原料需求 → 每种物质都有目标库存，据此决定派员与建造顺序。
- 四个关键策略（全部来自实测踩坑）：**电力节流**（电量够就 `unassign` 电站停烧省煤；开局 60 煤只够 150 秒）、**先电后煤再合金**（没单元时强行调离最低优先级设施，绝不放掉最后一台电站/煤矿）、**`recover` 前先确认付得起 `fixate`**、**收尾阶段只维护数据库相关产线**。
- `tools/blind_test.py` 增 `--policy ladder` 与 `--assert-victory`（未通关返回非 0，可当回归门禁）。
- **结果：20/20 局从零开局公平通关**（中位 47.8 游戏分钟、14 条主线全固化、记忆 100%、覆盖平均 22.2/26）。未覆盖的 4 项是失败模式或可选科技（`crash`/`renewable`/`unit_factory`/`fuel`），已如实记录。

**2. BUG-5：临时知识窗口（机制参数 + 体验）**
- **更正报告误判**："90 秒按墙钟走"不成立 —— `active_until` 用的是游戏时间，`agent_play` 暂停时看百科/思考都不流逝；真问题是 90 秒短于"抢执行单元 + 排队 + 建造"，且 `fixate` 拿不到单元就直接报错。
- 窗口 **90 → 180 游戏秒**（`content/recovery.json: temporary_ttl_default`，29 条统一，一处可调；条目仍可单独覆盖）。
- `fixate` **自动排队**：拿不到空闲单元不再报错，改为 FIFO 队列；材料**不预扣**（真开工才扣）；队列随存档读写（旧档无字段 → 空队列）。**排队不冻结窗口**（压力仍在），排队期间耗尽会写 warn 说明"重做要再付一次恢复材料"。
- **已开工的烧录不会中途丢失**：过期检查跳过有 `fixate` 作业在跑的条目。
- **倒计时与告警**：`entries` 显示「剩余 Ns」（<30s 标 `!`、排队中标注）；GUI 恢复树条目行同款（签名按 5 秒一档刷新）；剩 30s 写一次 warn（进警告看板）；`report.entries.active[]` 增 `expires_in` 与 `fixate_queued`；`suggest` 新增**最优先**规则"临时条目剩 ≤40s → 立刻 fixate"。

**3. 体验优化：`cover` 判据修复 + `suggest` 补 4 条规则**
- 修 `CoverageTracker.update` 的 `eval` 作用域**缺 `bool`**：`db_build` / `victory` / `env_event` 三个覆盖条目此前**永远判不过**，且异常被 `except` 静默吞掉（`cover` 的进度条长期虚低）。
- 修 `tools/blind_test.py` 的"命令被拒"判定：原来只看 `参数错误/未知指令`，业务错误（材料不足、没有空闲单元…）被当成成功 —— 策略会反复重试同一条指令并**饿死后面的步骤**。
- `suggest` 新增/改进 4 条：电力节流提示、缺合金先拆残骸（回收站免建造费）、恢复前先看固化费、钢富余且单元 <12 时建执行单元装配厂。
- `AGENTS.md`（由 `content/guide.json` 生成）规则 10 → 18 条：把阶梯策略实测出的 4 条要点写进向导，并补"自动通关基线"一条；主线条目数 13 → 14 修正。

### 二、影响范围
- **玩法数值**：只有一处 —— 临时知识窗口 90 → 180 游戏秒（外加"排队/开工保护"两条规则）；工艺链、成本、产出、记忆劣化系数**全部未动**。副作用：这一版整体**更容易**（压力仍在，但不易因处置不过来丢条目）。
- **接口**：`report` **只增字段**（`entries.active[].expires_in` / `fixate_queued`），`report_version` 不变；`cover`/`suggest`/`guide`/`entries` 输出增强，命令语法不变。
- **存档兼容**：`RecoverySystem.load` 对旧档（无 `queued`）无感，新增字段全部有默认值；`recovery.json` 的新键由 `main.py` 读取并带默认值。
- **界面**：恢复树条目行显示倒计时；`entries` 命令输出多一段剩余秒数；警告看板会多出"临时条目即将过期"这一类。
- **不改动**：战斗/敌人（本作没有）、内容规模、工艺链、终点判据。

### 三、验证
- **单测 206 例 OK**（193 + 新增 `tests/test_ladder_policy.py` 12 例 + `tests/test_recovery_window.py` 13 例；其中阶梯策略测试含"不许用 dbg/注入资源/依赖源码"的静态检查）。
- `python -X utf8 main.py --gui-selftest` 通过。
- 9 个端到端脚本全 OK：`smoke_test` / `gameplay_test`（真实施工耗时）/ `chain_test` / `power_test` / `tlb~tle_test` / `victory_test`（14 条主线 + 副产物处置 + 装配厂）。
- **盲测门禁**：`python -X utf8 tools/blind_test.py --policy ladder --runs 20 --seed-base 100 --max-minutes 300 --assert-victory` → **20/20 victory，退出码 0**；修复 BUG-5 后复跑仍是 20/20（证明没有把游戏改到失去压力）。
- 备份/回滚点：`_refactor_backup/ladder_policy_20260913-090526/`、`_refactor_backup/bug5_20260913-093401/`、`_refactor_backup/v1.3.2_20260913-094317/`。

</details>

---

## v1.4 — 电力调度 · 回收拆解 · 试玩修正

**日期**：2026-09-13
**归档**：`_refactor_backup/v1.4_20260913-164742/`
**发布快照**：`releases/v1.4/`
**主题**：把**电力**从"一起半速"做成可调度的系统（优先级降载 + 储能 + 容量上限），把**残骸**从"白给单元"改成部件加工链，并按一份完整试玩报告修掉一批 P0/P1 体验问题。

### 速览

- **电力调度（批次4）**：设施新增 `power_priority`（0 可选 / 1 生产 / 2 关键），缺电**从低到高降载**（不再全厂一起半速）；新设施**电池组 / 蓄热罐**（余电入库、缺口放电）；电力页加电网条、储能明细、降载档与电量时间线，环境页加可再生实时倍率。
- **A13 并入**：电网存量**上限 400kWh**（超出的发电量消纳不了 → 发电设施报「电网已满」降载且**不白烧燃料**）+ 0.05/s 自放电。
- **A12 ①③ + A14**：开局煤 **60 → 120**；**圈1 首次勘探保底出煤矿**；开局执行单元 **3 → 4**。
- **回收拆解（批次5）**：残骸**不再白给执行单元**，改为拆出**回收部件**（每 1 合金 0.3 件）；部件用于装配厂/维护站，过剩可经「部件加工车间」变回合金+钢。
- **试玩报告修正**：积压限产不再报成"输入不足"（`reason_code=backlog`）；电网满/复电抖动刷屏修掉（滞后 + 日志限流）；向导补「煤电双 0 不可逆死锁」与燃气发电机应急电源；`suggest` 秒数读真实窗口；关键资源采空给醒目告警；`agent_play` 改追加模式且不再重放旧命令。
- **§8 / §11**：记忆预警改为四级（60/30/15/8%）并带"还有几分钟归零"；`maintain` 成本不变；单元奖励上限 6 → **10**（改为 content 可配）。
- 单测 **263 例**。

<details>
<summary>详细更新报告（点击展开）</summary>

### 一、主要改动

**1. 批次4 电力优先级 + 储能（含 A13）**
- **4a** 设施 def 加 `power_priority`（16 台关键 + 19 台可选，其余默认 1）；缺电**从高到低分配**、低优先级先停（新停摆码 `shed`），替换原先"全厂按可用比例一起半速"。
- **4b** 新 `kind:"storage"`：**电池组**（400kWh / 3.0 每秒 / 90%）与**蓄热罐**（300kWh / 2.0 每秒 / 75%）；电网 ≥75% 容量充电、≤25% 放电；充放受"速率 / 电网余量 / 库容 / 不抽干"四重限制；电量记在设施 `stored`，随存档；待机不算停摆。
- **4c** 电力页：电网条、储能明细（状态/效率/速率）、降载档位、**电量时间线**（文本折线）；环境页：solar/pv/wind 实时倍率（D1）。
- **A13**：电网容量上限 400kWh + 0.05/s 自放电；发电受"消纳不了"限制（余量 < 2% 判满，涨回 15% 才恢复 —— **滞后**避免死区抖动），判满时报 `grid_full` 且不白烧燃料。
  - 实施踩坑记录：上限最初按 200 设，而稀土条目"恢复+固化"要 310 电 → 后期直接卡死（阶梯策略当场 0/20）。**上限必须高于最贵的主线恢复+固化**，已写进 `power.json` 注释。
- `report.power` 增 capacity/headroom/storage/shed/factors/demand/timeline；`report.facilities[].power_priority`；`suggest` 增"电网已满→建储能""降载→调优先级"。

**2. A12 ①③ + A14（用户决策）**
- 开局煤 **60 → 120**（电站 0.4/s 的窗口从 150 → 300 秒）。
- **圈1 首次勘探保底出煤矿**（`regions.json: guarantee`，按"世界里还没见过煤"判定，天然幂等、不需要新存档字段）。
- 开局执行单元 **3 → 4**（多一个"施工单元"）。

**3. 批次5 回收/拆解生态（A6 部件加工链 + B4）**
- 新物质 `salvage_part`「回收部件」；`salvager` **去掉** `reward_unit_on_deplete`，改成 `extract_extra`（每抽出 1 合金附带 0.3 件部件）。
- `assemble_unit` → 部件 0.03 + 钢 0.05 + 铜 0.03 + 电 0.5；`make_maintenance` → 部件 0.04 + 钢 0.03 + 合金 0.08；新增设施「部件加工车间」+ 配方 `recycle_parts`（部件 0.3 + 电 0.2 → 合金 0.22 + 钢 0.08，过剩出口）。
- `systems/industry.py` 只加一处机制：`extract_extra`（按主产物比例附带产出，同一效能/供电缩放、不吃储量）。
- B5（拆解返还）已由 P1 ③ 的 `demolish` 落地，本批补回归用例。

**4. 试玩报告（MODIFIABLE_PARTS_SUMMARY.md）P0/P1 修正**
- §3 积压限产：被限产设施现在带 `stall_reason=副产物积压限产（X 超限，产出 ×N）` 与 `stall_code=backlog`（state 仍是 running）；`status` 增「[积压]」行；`report.throttled`。
- §6 日志刷屏：电网满/复电加滞后 + `_report_stall` 日志限流（同设施同原因 20s 一条）；实测 2 分钟内电网满日志 < 8 条（原先单局 2.3 万行）。
- §1/§2 死锁与应急电源：向导加「⚠ 电网存量永远不要放空」（安全线 电≥120kWh、煤≥40t、电站别同开 >2 座）与燃气发电机自救序列（免科技、12 合金、建在**空地**）；`suggest` 增应急电源分支；百科新增 FAQ《煤电双归零怎么自救》。
- §4 `suggest` 写死"90s" → 改为读 `recovery.ttl_of()`；向导删掉不存在的 `fuel_options`，写明「燃煤电站是配方设施、固定烧煤、不能换燃料」。
- §5 矿脉枯竭：关键资源采空给醒目告警（含"还有 N 处可采"/"已无其他已知矿点"）。
- §7/§12 `agent_play`：输出默认**追加**（`--fresh` 才清空）、**不从 in 第 1 行消费**（跳过启动前旧命令，`--replay` 可重放）、锁文件按 mtime 软提醒、信息类命令**不截断**。
- §9 `recover` 时固化材料不足 → 显式警告（含真实窗口秒数）。
- §10/§13 可观测性：`report` 增 `plots_free_empty` / `depleting` / `throttled` / `flow`（关键资源产/耗/净）；`status` 显示可用空地数与积压限产。

**5. §8 记忆预警 + §11 单元奖励上限（用户决策）**
- §8 **维持现状 + 强化预警**：多级预警 60/30/15/8%（每档一次、回安全区重新武装），每条带"按当前劣化 X/s，约 N 分钟后归零"与建议；`warn_level()` / `seconds_to_crash()`；`report.memory.level` / `seconds_to_crash`；`suggest` 在 crisis/critical 时第一条给 maintain。**`maintain` 成本保持 20 电 + 5 合金 + 1 单元不变**。
  - 顺带修坑：预警判定原先在"回到安全区就 return"之后 → 60% 档永远不触发。
- §11 上限 6 → **10**，并把原先写死在代码里的 `unit_bonus_cap` 移到 `content/recovery.json: unit_bonus {per_mainline: 2, cap: 10}`。**算术说明**：14 条主线 ÷ 2 = 7，旧上限 6 会截掉 1 → 现在给满 7（开局 4 → 共 11 单元）。

**6. 顺带修复的既有缺陷（与上面无关，验收时暴露）**
- `balance_probe.py` 自 批次1.5 起就坏着：`build_staff` 建完**没等完工**就 `assign`，被"仍在建造中"拒掉 → 设施从不运转、第二部分一直报"早期供给未稳定"。现在等完工再派员（并修正 I1 的煤量）。
- `tools/agent_play.py` 的锁文件最初用 `os.kill(pid, 0)` 探测存活 —— **Windows 上这是 `TerminateProcess`，会真的杀掉那个进程**（在测试里就是自己）。已改为按锁文件 mtime 判新旧，全程不碰 pid，并全仓 grep 确认无其他 `os.kill`。

### 二、影响范围
- **玩法数值（本版改动最多的部分）**：电力上限/自放电/优先级降载（新机制）、开局煤 120、开局 4 单元、圈1 保底煤矿、残骸不再给单元、部件链配方、单元奖励上限 10、记忆预警分级（不改数值）。
- **难度净效果（如实记录）**：A14/A12 让**开局更松**；A6（残骸不再白给单元）让**中期更紧**，加上电网容量上限，阶梯策略的中位通关从 42 → **≈136 游戏分钟**（仍 20/20）。整体是"开局松、中期紧、更看重电力与单元投资"。
- **接口**：`report` 只增字段（memory.level/seconds_to_crash、power.*、plots_free_empty/depleting/throttled/flow、facilities[].power_priority），`report_version` 不变；新增停摆码 `shed` / `backlog` / `grid_full`。
- **存档兼容**：`recovery.unit_bonus` 缺失时用默认 {2,10}；`power.json` 新键都有默认值；`agent_play` 的锁文件不进存档。
- **命令行/工具**：`agent_play` 新增 `--fresh` / `--replay`（默认行为改为"追加 + 跳过旧命令"）。
- **不改动**：终局判据、敌人（本作没有）、工艺链主干、探索/环境机制。

### 三、验证
- **单测 263 例 OK**（新增 `tests/test_power_batch4.py` 19 + `tests/test_batch5_parts.py` 11 + `tests/test_playtest_fixes.py` 17 + `tests/test_warn_and_units.py` 10；`tests/test_memory.py` 的旧文案断言随 §8 同步更新）。
- `python -X utf8 main.py --gui-selftest` 通过。
- **10 个端到端脚本全 OK**：smoke / gameplay（真实施工耗时）/ chain / power（含电网满、储能充放、优先级）/ tlb~tle / victory（14 条主线）/ balance_probe（两段）。
- **盲测门禁**：`--policy ladder --runs 20 --seed-base 100 --max-minutes 300 --assert-victory` → **20/20，退出码 0**（中位 135.9 分钟、覆盖 23.6/27）。
- 定向验证（试玩报告 §14 要求）：死锁逃生（煤电双 0 + 煤气 → 燃气发电机救回）、积压文案区分（`backlog` ≠ `no_input`）、枯竭告警、秒数一致性（180s）—— 都在 `tests/test_playtest_fixes.py` 里。
- 备份/回滚点：`_refactor_backup/batch4_power_20260913-123645/`、`_refactor_backup/batch5_20260913-135734/`、`_refactor_backup/v1.4_20260913-164742/`。

</details>

---

## 未归档历史（仅备注，不追溯版本号）
- 1.0 里程碑：内核（时钟/经济/世界/单元/作业/注册表）+ M1 生产闭环 + M2 恢复树与真实化工链 + M3 胜利线（可靠数据库）。
- 1.1 里程碑：M4 拓展性验收（环境模块）+ TL-0 副产物与发电生态 + TL-A 电气化 + 若干 UI 重构（面板拆分、标签页容器）。
